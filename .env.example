# ─── Vercel KV (auto-populated when you connect a KV database in Vercel dashboard) ───
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ─── Local dev only (vercel dev injects these automatically from your linked project) ──
# No manual values needed — credentials are stored per-user via /api/credentials
